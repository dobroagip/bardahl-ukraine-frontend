# Bardahl Ukraine - Инструкция по эксплуатации

## 🚀 Вариант 3: Деплой на собственный VPS (Ubuntu)

Этот метод рекомендуется для максимальной скорости работы в Украине.

### 1. Подготовка сервера
Зайдите на ваш сервер по SSH и выполните:
```bash
sudo apt update && sudo apt install nginx rsync -y
sudo mkdir -p /var/www/bardahl
sudo chown -R $USER:$USER /var/www/bardahl
```

### 2. Настройка Nginx
1. Создайте конфиг: `sudo nano /etc/nginx/sites-available/bardahl`
2. Скопируйте содержимое файла `nginx.conf` из этого репозитория.
3. Активируйте сайт:
```bash
sudo ln -s /etc/nginx/sites-available/bardahl /etc/nginx/sites-enabled/
sudo rm /etc/nginx/sites-enabled/default
sudo nginx -t && sudo systemctl restart nginx
```

### 3. Автоматизация деплоя
На вашем локальном компьютере:
1. Отредактируйте `deploy.sh` (укажите IP вашего сервера).
2. Разрешите запуск скрипта: `chmod +x deploy.sh`
3. Запускайте его каждый раз, когда хотите обновить сайт: `./deploy.sh`

### 4. Настройка SSL (HTTPS)
Бесплатный сертификат Let's Encrypt:
```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d your-domain.com
```

## ⚠️ Важное замечание по безопасности
В файле `services/notificationService.ts` используются прямые вызовы API Resend и Telegram. 
**В продакшене:** Рекомендуется проксировать эти вызовы через простой Node.js сервер или Cloudflare Workers, чтобы скрыть API-ключи от просмотра в коде страницы через DevTools браузера.